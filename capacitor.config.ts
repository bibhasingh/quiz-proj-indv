import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'quiz-proj-indv',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
